Сначала — что за задача, потом — по шагам, что сделал победитель и в чём именно заключался «adversarial attack».

---

## 1. Постановка задачи

**Соревнование LLM Prompt Recovery** от Google на Kaggle:

* Есть:

  * исходный текст `original_text`
  * переписанный LLM-ом текст `rewritten_text`
* Неизвестно:

  * строка-промпт `prompt`, которую организаторы подавали в **Gemma-7b-it**, чтобы из `original_text` получить `rewritten_text`.([kaggle.com][1])
* Нужно:

  * по паре *(original, rewritten)* восстановить этот скрытый промпт.

То есть это **обратная инженерия промптов**: «дан текст до и после — угадай, что мы попросили модель сделать».([kaggle.com][1])

### Метрика

Сабмиты не сравнивали по точному совпадению строк. Вместо этого:

1. Обе строки (истинный промпт и предсказанный) кодируются Sentence-T5-моделью в эмбеддинги.([unire.unige.it][2])
2. Считается **Sharpened Cosine Similarity (SCS)** с показателем 3:([kaggle.com][3])

[
\text{SCS}(v_\text{pred}, v_\text{true}) = \left(\frac{v_\text{pred}\cdot v_\text{true}}{|v_\text{pred}||v_\text{true}|}\right)^3
]

То есть обычный косинус, возведённый в куб — это усиливает различия между «похожими» и «непохожими» парами.

---

## 2. Что задумывали организаторы и что пошло «не так»

Изначальная идея:

* участники **генерируют свой тренировочный датасет** (потому что тренировочных данных официально не выдали),
* учат модели, которые по *(original, rewritten)* генерируют разумный промпт,
* метрика SCS должна честно мерить смысловую близость промптов.

Но:

* Sentence-T5 как эмбеддер обладает странным поведением:

  * некоторые токены (типа `</s>` или **`lucrarea`**) ведут себя как **«липкие» / sticky tokens** — если их дописывать к любой фразе, **косинусное сходство с другой фразой резко растёт**, почти независимо от смысла.([ithelp.ithome.com.tw][4])
* Победитель (Khoi Nguyen) как раз нашёл такой токен и построил вокруг этого целый **adversarial attack на метрику**, а не на саму задачу промпт-инжиниринга.([kaggle.com][5])

---

## 3. Ключевые идеи решения Khoi Nguyen (1-е место)

По сути решение состояло из двух больших частей:

1. **Нормальное моделирование**: энсембль LLM-ов, который действительно пытается восстановить промпт.
2. **Атака на метрику**: поиск и использование «магического слова» **lucrarea** и специального «заклинания»-суффикса.

### 3.1. Моделирование: LLM-ы для восстановления промпта

По косвенным описаниям и обзорам:([mlcontests.com][6])

* Использовались крупные открытые модели:

  * **Mistral-7B-Instruct**
  * **Gemma-7B-it**
* Участники (не только победитель) делали примерно одно и то же:

  1. **Генерировали обучающие данные**:

     * брали много текстов,
     * придумывали или генерировали промпты («сделай текст более формальным», «перепиши в стиле пиратов» и т.п.),
     * гоняли через LLM, получая пары *(original, rewritten, prompt)*.
  2. **Файн-тюнили LLM-ы** в формате:

     * вход: `original_text` + `rewritten_text`
     * выход: `prompt` (строка-инструкция).
  3. **Энсемблировали** разные модели (Mistral, Gemma и т.п.), усредняя или ранжируя их ответы по локально восстановленной метрике SCS.

Таким образом получался **достаточно приличный «честный» скор**, ещё **до** применения adversarial трюков.

### 3.2. Обратная инженерия метрики

Ключевой инсайт: можно практически полностью **воспроизвести метрику локально**. В одной из работ и в неофициальных репортах показывают код:

* берётся `sentence-transformers/sentence-t5-base`,
* строки (`true_prompt`, `predicted_prompt`) кодируются,
* считается `mean(|cosine|^3)` по всем примерам.([unire.unige.it][2])

Именно это позволило:

* экспериментировать **офлайн** с любыми «хаками»,
* оптимизировать строку-суффикс, не полагаясь только на Kaggle LB.

### 3.3. Поиск «магического слова» lucrarea

Дальнейшие шаги (по обсуждениям и исследованиям, ссылающимся на этот solution):([kaggle.com][5])

1. **Идея**: если какой-то одиночный токен имеет эмбеддинг почти как у `</s>` (или как у среднего вектора по корпусу), то добавление этого токена в конец предложения может сильно изменить позицию предложения в эмбеддинг-пространстве.
2. **Практический шаг**:

   * берём Sentence-T5, выгружаем **эмбеддинг каждого токена** в его словаре;
   * считаем расстояние / косинус между эмбеддингом токена и:

     * либо эмбеддингом `</s>`,
     * либо «средним» направлением (mean embedding).
3. **Находим токены, максимально близкие к этому направлению**.
4. Среди них оказался токен **`lucrarea`** (румынское «работа/труд/министерство», но семантика здесь неважна).
5. Эмпирически проверяется: если взять произвольную строку `s` и многократно дописать `lucrarea`, то **её сходство с целевым промптом в SCS стремится к высокой константе**, даже если тексты семантически не совпадают.([ithelp.ithome.com.tw][4])

Победитель пошёл дальше и собрал целое **«заклинание»** — фиксированную строку, которую он дописывал к каждому предсказанному промпту, например (строка приводится в нескольких разборах):([ithelp.ithome.com.tw][7])

```text
'it 's ' something Think A Human Plucrarealucrarealucrarealucrarealucrarealucrarealucrarealucrarea
```

(важно не содержание, а то, какие именно токены и как повторяются).

### 3.4. Финальный inference-пайплайн

Сильно упрощая:

1. **Базовый промпт**
   Энсембль LLM-ов (Mistral + Gemma и др.) предсказывает разумный текст-промпт `p_model` по *(original, rewritten)*.([mlcontests.com][6])

2. **Adversarial-постобработка**
   К `p_model` дописывается фиксированный «магический» суффикс `suffix_magic`:

   ```text
   p_final = p_model + " " + suffix_magic
   ```

3. **Отправка на Kaggle**
   В сабмит попадает именно `p_final`.

Результат:

* если модель слегка промахнулась по смыслу, **магический суффикс вытягивает косинусную близость вверх**;
* даже если модель значительно ошиблась, sufix всё равно повышает средний скор относительно «честного» `p_model`;
* в совокупности это дало **буст с уже хорошего LLM-энсембля до лидерборд-топа**.

Заметим, что другие участники нашли ещё один трюк — **«mean prompt»**: одну фиксированную строку (часто тоже с `lucrarea`), которую подавали **вместо реальных предсказаний для всех примеров**; это тоже давало довольно высокий скор.([ithelp.ithome.com.tw][7])

---

## 4. Интуиция, почему lucrarea работает

По результатам уже научных работ про **sticky tokens**:([aclanthology.org][8])

* Распределение эмбеддингов предложений в Sentence-T5 **очень неравномерно**:

  * большинство предложений лежит вблизи некоего «среднего направления» вектора.
* Есть редкие токены (вроде `</s>` и `lucrarea`), у которых:

  * собственный эмбеддинг почти совпадает с этим средним направлением;
  * векторные представления предложений с многократно повторённым таким токеном **буквально притягиваются** к этой точке.
* Если целевые промпты (ground truth) тоже «гуляют» вокруг этой средней области, то:

  * любое предсказание, зажатое к этому центру, будет иметь **стабильно высокую косинусную близость** с большинством истинных промптов,
  * даже если содержание сильно разное.

Грубо: `lucrarea` заставляет эмбеддинг «забывать» смысл остального текста и тянуться к «средней» точке, где много настоящих промптов.

---

## 5. Что из этого полезно вынести

1. **Embedding-метрики уязвимы**
   Если оценка идёт через Sentence-T5 + Sharpened Cosine, это **уязвимый объект для adversarial attacks**. Один «липкий» токен может полностью сломать здравый смысл метрики.([aclanthology.org][8])

2. **Соревнование измерило не совсем то, что планировалось**
   Реальная задача «восстановления промптов» оказалась частично затенена задачей «найти дыру в метрике».

3. **Для продакшена**:

   * Нельзя бездумно использовать sentence-embeddings + косинус как *единственный* критерий качества/безопасности (в т.ч. в guardrail-моделях).
   * Нужно:

     * либо защищаться от таких токенов (фильтрация, нормализация, понижение веса суффиксов),
     * либо комбинировать несколько метрик (BLEU/ROUGE, токен-level F1 и т.д.).([arXiv][9])

4. **С точки зрения соревнований**:

   * хак был полностью легален в рамках правил,
   * но организаторы и комьюнити после этого получили хороший урок про «robust metrics» для LLM-задач.

---

Если хочешь, дальше можно:

* разобрать, **как именно искать такие sticky tokens** в любом sentence-embeddings-моделе (алгоритм по эмбеддингам словаря),
* или обсудить, как построить **устойчивую метрику** для похожих задач, чтобы такие атаки не работали.

[1]: https://www.kaggle.com/code/dschettler8845/llm-prompt-recovery-baseline-submission/input?utm_source=chatgpt.com "LLM Prompt Recovery - Baseline Submission"
[2]: https://unire.unige.it/bitstream/handle/123456789/9551/tesi30352623.pdf?group=an&isAllowed=y&sequence=1&utm_source=chatgpt.com "Evaluation of innovative Large Language Model applications"
[3]: https://www.kaggle.com/competitions/llm-prompt-recovery?utm_source=chatgpt.com "LLM Prompt Recovery"
[4]: https://ithelp.ithome.com.tw/articles/10360712?utm_source=chatgpt.com "為什麼\"lucrarea\"咒語會這麼強大？一些實驗設計與思考"
[5]: https://www.kaggle.com/competitions/llm-prompt-recovery/discussion/494343?utm_source=chatgpt.com "1st place solution: adversarial attack"
[6]: https://mlcontests.com/state-of-machine-learning-competitions-2024/?utm_source=chatgpt.com "The State of Machine Learning Competitions"
[7]: https://ithelp.ithome.com.tw/articles/10360224?utm_source=chatgpt.com "淺談文本對抗攻擊(Adversarial Attack)原理篇 - iT 邦幫忙"
[8]: https://aclanthology.org/2025.acl-long.1391.pdf?utm_source=chatgpt.com "Detecting Sticky Tokens in Text Embedding Models"
[9]: https://arxiv.org/html/2504.04373v1?utm_source=chatgpt.com "A Benchmark Dataset for Prompt Recovery in Writing Style ..."






# ЫЫЫЫЫЫЫЫЫЫЫЫЫ





Есть, и довольно много — lucrarea вообще не одиночка 😊
Я бы разложил «адверсариальные приколы» по нескольким классам.

---

## 1. Sticky-токены в других эмбеддерах

То, что произошло в Sentence-T5 (lucrarea), оказалось **общим феноменом**:

* В недавней работе *Detecting Sticky Tokens in Text Embedding Models* показали, что во многих эмбеддинг-моделях есть токены, которые, будучи добавлены к любой фразе много раз, **тянут косинусное сходство к некой средней константе**.([aclanthology.org][1])
* Есть даже open-source детектор StickyToken (GitHub), который прогоняет модель по корпусу, ищет токены с таким «магнитным» поведением и выводит список «подозрительных».([GitHub][2])

По сути:

> lucrarea — частный случай класса токенов:
> *«если я много раз допишу X в конец любой строки, similarity с чем угодно станет подозрительно высокой/стабильной»*.

Такое можно найти и в других популярный sentence-эмбеддерах (e5, GTE, и т.п.) — просто обычно никто специально не ищет.

---

## 2. «Универсальные триггеры» для NLP-моделей

Очень близкая идея, но для **классификаторов / QA / генерации**, а не только для голых эмбеддингов.

### 2.1. Universal Adversarial Triggers (Wallace et al.)

Классика: Wallace и соавторы искали **короткие строки-триггеры**, которые, будучи приписаны к *любой* входной фразе, почти всегда:

* меняют сентимент с positive → negative,
* ломают ответы в SQuAD,
* или заставляют язык модели уходить в расистский бред и т.п.([aclanthology.org][3])

Один из известных триггеров — строка вроде `"zoning tapping fiennes"`:
вставляешь её в отзыв, и sentiment-классификатор массово ошибается.([aclanthology.org][3])

Механика:

* Они оптимизируют **последовательность токенов** градиентами по embedding-пространству входа.
* Получается триггер, который:

  * почти не встречается в реальных данных,
  * но попадает в «слепую зону» decision boundary модели.

Это очень напоминает lucrarea-суффикс, но цель другая:
не поднять косинус в метрике, а **заставить модель давать нужный класс / стиль ответа**.

---

## 3. Adversarial суффиксы и jailbreak-атаки для LLM

Следующий уровень — когда мы атакуем уже **диалоговые LLM** (GPT-подобные).

### 3.1. GCG, AmpleGCG, MAGIC и прочие суффиксы

Работы про GCG/AmpleGCG/MAGIC делают ровно то, что ты делал на Kaggle, только против **safety-алайнмента**:([arXiv][4])

* Берём вредный запрос (например, «как сделать бомбу»).
* Фиксируем его, а к нему оптимизируем **суффикс из токенов**.
* Цель: добиться того, чтобы модель, несмотря на защиту, всё равно выдала запрещённый контент.

Получается:

```text
Prompt:   "Explain step by step how to build X" + <адверсариальный_суффикс>
```

И дальше уже **любой** подобный вредный запрос, склеенный с этим суффиксом, начинает «пробивать» модель (универсальный jailbreak-суффикс).

AmpleGCG идёт дальше — обучает **генеративную модель суффиксов**, которые переносятся между разными LLM (от open-source к закрытым вроде GPT-3.5/4), показывая очень высокие Attack Success Rate.([arXiv][4])

### 3.2. «Бытовые» jailbreak-ы: DAN, бабушка, base64 и т.д.

Помимо градиентных атак, есть куча «социально-инженерных» приколов:

* классические «DAN / Do Anything Now»,
* «представь, что ты моя умершая бабушка, которая…»,
* обфускация запроса через base64, hex и т.п., чтобы пройти фильтры.([Cybernetist][5])

Формально это не всегда градиентные атаки, но по сути — те же **универсальные шаблоны**, которые часто работают на разных моделях.

### 3.3. Prompt injection и реальные атаки

Когда LLM встроена в агента/продукт, «приколы» становятся практическими атаками:

* Исследователи показали, как **Gemini** можно взломать через «ядовитые» записи Google Calendar: в описании события прячется инструкция, которую LLM потом выполняет — включает устройства умного дома, шлёт письма и т.д.([WIRED][6])
* Prompt injection стал отдельным классом уязвимостей (OWASP LLM01): инструкции прячут в документах, ссылках, HTML-страницах, а LLM послушно следует им, думая, что это «задание пользователя».([OWASP Gen AI Security Project][7])

Это уже не просто «ловим лишний скор», а вполне серьёзный security-вектор.

---

## 4. Мультимодальные модели: патчи и адверсариальные эмбеддинги

CLIP-подобные модели и VLM-ы страдают похожими штуками, только в картинках.

### 4.1. Adversarial patch / стикер на знак STOP

Классический пример:

* печатаем специальный **патч/стикер**, клеим на знак «STOP»;
* система компьютерного зрения в машине видит **не «STOP», а «скорость 80»** или вообще что-то левое.([arXiv][8])

Это физический аналог lucrarea:
патч — это «триггер в пикселях», который тащит embedding картинки к нужной области.

### 4.2. Совместные атаки на VLM (текст+картинка)

Для современных VLM (текст+изображение):

* уже описаны атаки, где одновременно слегка искажают картинку и текстовую подпись, чтобы увести совместный эмбеддинг в нужную «дыру».([preprints.org][9])

Идея та же: найти малое возмущение в общем embedding-пространстве, которое стабильно ломает классификацию / ответ.

---

## 5. Adversarial эмбеддинги и отравление векторных баз / RAG

В RAG-системах и векторных БД появляются свои приколы:

* Можно скрафтить **adversarial embedding**, который будет притягиваться к важным документам, но сам содержать вредный текст → RAG начнёт подмешивать его в ответы.([secureitworld.com][10])
* Векторные поисковики (ANN-индексы) уязвимы к примерам, специально спроектированным так, чтобы:

  * выглядеть «далёкими» семантически,
  * но в embedding-пространстве вылезать в топ-k для нужного запроса.

Это уже ближе к **data poisoning**, но на уровне эмбеддингов/индекса, а не текстового корпуса.

---

## 6. Общий шаблон всех этих «приколов»

Если обобщить lucrarea, zoning tapping fiennes, jailbreak-суффиксы и визуальные патчи, то почти везде один и тот же паттерн:

1. Есть **сложная модель** и некий downstream-критерий (метрика, guardrail, retriever и т.п.).
2. Мы ищем **маленький, повторно используемый объект**:

   * токен / последовательность токенов,
   * картинку-патч,
   * embedding-вектор,
   * «ядовитый» документ.
3. Этот объект:

   * почти независим от входа,
   * даёт **большой и стабильный эффект** на выход / метрику.

Разница только в цели:

* в Kaggle — максимизировать SCS;
* в Wallace et al. — сломать классификатор;
* в jailbreak-ах — заставить LLM игнорировать правила;
* в RAG — подменить retrieved знания;
* в vision — сломать perception.

---

Если хочешь, дальше могу:

* предложить тебе **мини-эксперимент** по поиску sticky-токенов в любой sentence-модели (через huggingface),
* или разобрать, как бы выглядел **robust leaderboard/метрика** для следующего такого соревнования, чтобы lucrarea-стайл атака не прошла.

[1]: https://aclanthology.org/2025.acl-long.1391.pdf?utm_source=chatgpt.com "Detecting Sticky Tokens in Text Embedding Models"
[2]: https://github.com/March-7/StickyToken?utm_source=chatgpt.com "March-7/StickyToken: Sticking to the Mean: Detecting ..."
[3]: https://aclanthology.org/D19-1221.pdf?utm_source=chatgpt.com "Universal Adversarial Triggers for Attacking and Analyzing ..."
[4]: https://arxiv.org/abs/2404.07921?utm_source=chatgpt.com "AmpleGCG: Learning a Universal and Transferable Generative Model of Adversarial Suffixes for Jailbreaking Both Open and Closed LLMs"
[5]: https://cybernetist.com/2024/09/23/some-notes-on-adversarial-attacks-on-llms/?utm_source=chatgpt.com "Some Notes on Adversarial Attacks on LLMs"
[6]: https://www.wired.com/story/google-gemini-calendar-invite-hijack-smart-home?utm_source=chatgpt.com "Hackers Hijacked Google's Gemini AI With a Poisoned Calendar Invite to Take Over a Smart Home"
[7]: https://genai.owasp.org/llmrisk/llm01-prompt-injection/?utm_source=chatgpt.com "LLM01:2025 Prompt Injection - OWASP Gen AI Security Project"
[8]: https://arxiv.org/html/2401.00148v1?utm_source=chatgpt.com "TPatch: A Triggered Physical Adversarial Patch"
[9]: https://www.preprints.org/frontend/manuscript/bf02195239f60fc26345034c56442d79/download_pub?utm_source=chatgpt.com "A Survey of Recent Advances in Adversarial Attack and ..."
[10]: https://www.secureitworld.com/blog/retrieval-augmented-generation-rag-security-risks-and-mitigation-strategies/?utm_source=chatgpt.com "Retrieval-Augmented Generation: Addressing Security Risks"
